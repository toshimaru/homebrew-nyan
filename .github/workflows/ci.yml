name: Brew Check
on: [push]
jobs:
  build:
    runs-on: macos-11
    steps:
    - uses: actions/checkout@v3
    - name: brew audit/install/test
      run: |
        brew audit ./*.rb
        brew install -v ./*.rb
        brew test ./*.rb
